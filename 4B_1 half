#include <stdio.h>
#include <stdlib.h>
//WAP to accept Employee grade and salary and display the net salary.
int main()
{
   
  int grade;
  float salary, HRA, DA, PF, net _salary;
  
  printf("Enter the salary of the employee: ");
  scanf("%f", &salary);
  
  printf("Enter the grade of the employee: ");
  scanf("%d", &grade);
  
  if(grade==1)
  {
      HRA=0.5*salary;
      DA=0.2*salary;
      PF=0.05*salary;
  }
  else if(grade==2)
  {
      HRA=0.4*salary;
      DA=0.3*salary;
      PF=0.03*salary;
  }
  else if(grade==3)
  {
      HRA=0.3*salary;
      DA=0.15*salary;
      PF=
  }
}
