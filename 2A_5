#include <stdio.h>
#include <stdlib.h>
//Take two numbers from the user and display the largest.
int main()
{

    int firstnumber, secondnumber;
    printf("\nEnter the first number: ");
    scanf("%d", &firstnumber);
    printf("\nEnter the second number: ");
    scanf("%d", &secondnumber);
    if(firstnumber>secondnumber)
    {
        printf("\nThe first number, %d, is greater than the second number, %d\n", firstnumber, secondnumber);
    }
    else if(secondnumber>firstnumber)
    {
        printf("\nThe first number, %d, is less than the second number, %d\n", firstnumber, secondnumber);
    }
    else
    {
        printf("\nThe first number, %d, is equal to the second number, %d.\n", firstnumber, secondnumber);
    }
}
